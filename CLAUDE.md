# CLAUDE.md — 潮目盤（暗号資産ダッシュボード）

このファイルは Claude Code が毎回読む常設指示です。作業前に必ず目を通してください。

## このプロジェクトは何か

ビットコインと主要アルトコインの値動き・資金の流れ・季節性統計を1ページで確認するための
静的Webサイトです。個人（のんた）が毎朝ブラウザで開いて相場の環境認識をする用途で、
売買シグナルの配信や投資助言を目的としたものではありません。

対象銘柄は5つに固定します。増やす指示があるまで勝手に追加しないでください。

| 表示名 | シンボル | CoinGecko ID | 取引所シンボル |
|---|---|---|---|
| ビットコイン | BTC | bitcoin | BTCUSDT |
| イーサリアム | ETH | ethereum | ETHUSDT |
| リップル | XRP | ripple | XRPUSDT |
| ビルドアンドビルド | BNB | binancecoin | BNBUSDT |
| ソラナ | SOL | solana | SOLUSDT |

## 技術方針（重要）

- **ビルドツールなし。** npm も webpack も Vite も使いません。素の HTML / CSS / JavaScript のみ。
  `index.html` をブラウザで開けばそのまま動く状態を常に維持してください。
- **フロントエンドに外部ライブラリを入れない。** チャートは SVG を直接組み立てます。
  Chart.js / D3 / TradingView ウィジェットの導入は提案しないでください。
- **npm を使うのは `scripts/` 以下の Node スクリプトだけ**（依存ゼロ、Node 18+ の標準 fetch を使用）。
- API キーをフロントエンドのコードに絶対に書かないこと。鍵は GitHub Secrets のみ。
- `localStorage` は設定の保存だけに使用。価格データはキャッシュしない。

## データの出どころ

| 用途 | 取得先 | 備考 |
|---|---|---|
| 現在価格・騰落率・時価総額・7日スパークライン | CoinGecko `/coins/markets` | 無料・キー不要。1回の呼び出しで5銘柄ぶん取得 |
| 30日/1年の折れ線 | CoinGecko `/coins/{id}/market_chart` | 呼びすぎると429。240ms間隔・結果はメモリキャッシュ |
| ドミナンス現在値・市場全体の時価総額 | CoinGecko `/global` | 過去の時系列は無料枠で取れない |
| ローソク足（日足・週足・月足） | Binance `/api/v3/klines` → 失敗時 Bybit `/v5/market/kline` | USDT建て。地域制限で落ちることがあるため必ずフォールバックを維持 |
| 2015〜2017年前半の月次騰落率 | Bitstamp `/api/v2/ohlc/btcusd` | Binanceの月足は2017年8月開始。ここで前半を補完 |
| 今日のトピック | `data/topics.json` | GitHub Actions が Anthropic API + web_search で毎朝生成 |

**ドミナンスの過去推移は概算の参考データ**（`DOM_REF` 定数）です。実測ではないことを
画面の注記から削除しないでください。実測に置き換えられる無料ソースを見つけた場合のみ差し替え可。

## 表示のルール

- **赤＝上昇、青＝下落**（日本式）。欧米式の緑赤に変えないこと。
- 数値は等幅フォント＋`font-variant-numeric: tabular-nums`。桁がずれる表示は不可。
- 通貨は円がデフォルト。ローソク足はUSDT建てなので、BTCの円/ドル比から概算換算する。
- 統計には必ずサンプル数を併記する。勝率だけを大きく見せない。
- 断定的な予測・推奨の文言を書かない（「〜すべき」「必ず上がる」など）。
  傾向は「過去の集計では」と前置きし、免責の一文をフッターから消さない。

## デザイントークン

CSS変数は `:root` に集約済み。新しい色を直書きせず、既存の変数を使ってください。

```
--ground #0E1626   背景（深藍）
--panel  #141F35   パネル
--ink    #F1EDE3   文字
--up     #F2584B   上昇（朱）
--down   #4DA3E0   下落（藍）
--btc    #E0A93B   ビットコイン基準線
--ma25   #F0C24B   25本移動平均
--ma75   #7FD4C1   75本移動平均
```

見出しは明朝体（`--mincho`）、本文はゴシック（`--gothic`）、数値は等幅（`--mono`）。
Webフォントは読み込まず、OS標準の日本語フォントで組みます。

## 画面の構成（この順番を維持）

1. 対ビットコイン潮位 — BTCを0点に、各アルトの相対位置を1本の軸に並べる看板要素
2. 銘柄一覧カード — 価格・騰落率・スパークライン・時価総額
3. 重ね合わせ — 期間始点を0%に揃えた比較折れ線
4. ローソク足 / マルチタイムフレーム — 日足⇄週足、ローソク⇄ライン、25/75本線、両時間軸の状態バッジ
5. ビットコインドミナンス
6. ビットコインの月別成績 — 勝敗・勝率・平均騰落率＋年×月ヒートマップ
7. ビットコイン→アルトの順で上がった確率
8. 注目テーマと今日のトピック

## よく使うコマンド

```bash
# ローカル確認（file:// だと fetch が制限されるので必ずサーバ経由で開く）
python3 -m http.server 8000
# → http://localhost:8000

# トピックを手元で生成
export ANTHROPIC_API_KEY=sk-ant-...
node scripts/fetch-topics.mjs

# 構文チェック（依存なし）
node --check scripts/fetch-topics.mjs
```

## 作業のしかた

- 変更は小さく刻み、1つ直すたびにブラウザで確認できる状態に戻すこと。
- 大きな改修の前に、何をどう変えるかを箇条書きで先に示して合意を取ること。
- ネットワークが落ちたときの表示（エラー文言・再試行ボタン・スケルトン）を壊さないこと。
  API制限（429）とオフラインは別のメッセージを出し分けます。
- モバイル幅600px以下でも崩れないこと。キーボード操作のフォーカス表示を消さないこと。
- `prefers-reduced-motion` の指定を消さないこと。
- 疑わしい数値ロジックを直したときは、`src/prototype.html`（初版の丸ごと控え）と
  結果を比べて差分の理由を説明すること。
