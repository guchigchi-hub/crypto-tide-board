# Claude Code への指示文

上から順に、1つずつ貼って進めてください。前のステップが終わってから次に進みます。

---

## 0. 準備（ターミナルで実行）

```bash
cd ~/Documents        # 置きたい場所へ
# ダウンロードした crypto-tide-board フォルダをここに展開しておく
cd crypto-tide-board
git init && git add -A && git commit -m "init: 潮目盤の初版"
claude
```

---

## 1. 現状把握（最初に貼る）

```
CLAUDE.md を読んでから、このプロジェクトの現状を確認してください。

- index.html が単一ファイルで完結した状態であること
- ローカルサーバで開いて、8つのセクションがすべて描画されること

python3 -m http.server 8000 を起動して、
私が http://localhost:8000 で確認できる状態にしてください。
確認できたら、気になった点を3つまで挙げてください。まだ何も直さなくて構いません。
```

---

## 2. ファイル分割（最初の実作業）

```
index.html が1100行を超えて編集しづらいので、機能ごとに分割してください。

- assets/styles.css        … <style> の中身をそのまま移す
- assets/config.js         … 銘柄定義・期間定義・DOM_REF・THEMES などの定数
- assets/data.js           … CoinGecko / Binance / Bybit / Bitstamp の取得処理
- assets/charts.js         … SVG描画（潮位計・重ね合わせ・ローソク足・ドミナンス）
- assets/stats.js          … 月別集計とローテーション確率の計算
- assets/app.js            … 状態管理・イベント・起動処理

条件:
- ビルドツールは使わない。<script type="module"> と import/export で繋ぐ
- 見た目と挙動は1ミリも変えない。分割だけを行う
- 分割後、ブラウザのコンソールにエラーが出ないことを確認してから報告する
- src/prototype.html は分割前の控えなので触らない
```

---

## 3. 動作確認とバグ潰し

```
以下を1つずつ確認して、崩れていれば直してください。直したら何をどう直したか教えてください。

1. 期間ボタン（24時間 / 7日 / 30日 / 1年）を切り替えたとき、
   潮位計・カード・重ね合わせの3つがすべて連動するか
2. 円 / ドルを切り替えたとき、ローソク足の価格軸も換算されるか
3. ローソク足で銘柄・日足週足・ローソクライン・本数を切り替えたとき、
   25本線と75本線が正しく引き直されるか
4. 月別統計テーブルが2015年から集計されているか。
   Bitstampの取得に失敗した場合は2017年8月からになるので、
   その場合は「集計開始が2017年8月になっている」旨を画面に出す
5. スマホ幅（375px）で横スクロールが発生していないか
```

---

## 4. トピックの自動更新をつなぐ

```
data/topics.json を GitHub Actions で毎朝更新する構成になっています。
scripts/fetch-topics.mjs と .github/workflows/daily-topics.yml を読んで、
以下を確認・修正してください。

- index.html（分割後は app.js）が data/topics.json を最優先で読み、
  無ければ常設テーマだけを表示する流れになっているか
- topics.json の generatedAt が今日でない場合、
  「◯月◯日時点」と正直に日付を出す（古いものを今日として出さない）
- ローカルで一度だけ実行して動作確認したい。
  ANTHROPIC_API_KEY を環境変数で渡す手順を教えてください
```

---

## 5. 公開

```
GitHub に上げて GitHub Pages で公開したいです。

- リポジトリ名は crypto-tide-board、公開設定は private
  （Pages を使う場合の注意点があれば教えてください）
- .github/workflows/deploy.yml がそのまま使えるか確認
- ANTHROPIC_API_KEY を Secrets に登録する手順を、画面の場所まで具体的に

コマンドは私が実行するので、順番に1つずつ提示してください。
```

---

## 6. その後に頼めること（好きなものを選んで）

```
ローソク足に出来高の移動平均を重ねてください。既存の配色から外れないように。
```

```
潮位計の下に「アルトシーズン指数」を追加したい。
上位50銘柄のうち90日でBTCを上回った銘柄の割合として、CoinGeckoの無料APIだけで計算できますか。
呼び出し回数と429のリスクを見積もってから、可否を判断して提案してください。
```

```
月別統計に、半減期からの経過月数で色分けする列を追加してください。
半減期は 2012-11-28 / 2016-07-09 / 2020-05-11 / 2024-04-20 です。
```

```
毎朝7時に、その日の潮位・ドミナンス・トピックを1枚の画像にして
Discord の指定チャンネルへ送る仕組みを作りたい。設計案を3つ出してください。
```
